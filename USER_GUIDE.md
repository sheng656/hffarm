# 🌿 HF 农场成品菜管理系统 — 员工使用手册

本系统用于登记农场每日成品菜的收成、打板以及大棚分类统计。请仔细阅读以下使用说明，方便日常顺利录入和对账。

---

## 📱 如何登录

1. 在手机或电脑浏览器中打开系统网址。
2. 输入你的**邮箱地址**和**密码**。
3. 点击「登录」。

> 💡 **初始密码**：`HFfarm2026`
> 首次登录后，建议前往「更多」→「个人设置」修改为自己的安全密码。

---

## 📋 核心功能与操作指南

### 1. ➕ 板级数据录入 (新增)

**位置**：点击底部导航栏的「录入」按钮

系统全新升级为**按板录入**，支持同一块板（Pallet）放置多个团队采收的不同产品。步骤如下：

1. **选择板型 (Pallet)**：
   - 在顶部选择打板类型（**NPO / RED / FCC**）。**默认状态是不选中的**，请务必根据实际物理板点选。
2. **在该板上添加产品明细**：
   - **选择产品**：在搜索框中模糊搜索产品名称并点选。
   - **团队**：点击所在的采收团队（H / J / M / S / W / Y）。
   - **箱型**：点击选择本批记录使用的箱型（如 L47, B37 等）。
   - **大棚编号**：输入或选择实际采收的大棚编号（如 GH04、户外WF03、外采、进口等）。
   - **数量类型**：点击高亮切换 **Bag (包数)** 或 **Loose (散数)**，配合 `+` / `-` 步进器或键盘输入数量。
   - **备注**（选填）：如有需要可写备注。
   - 点击 **「添加到当前板」**，产品会被暂存到当前板明细列表中。
   - 可继续在该板上重复此步骤，添加下一个产品。
3. **提交整板**：
   - 检查已添加的产品列表无误后，点击底部的 **「确认提交整板」** 按钮，系统会一次性将整板和明细数据写入数据库。

> ⚠️ **注意**：
> - 录入前需先选板型，否则无法添加产品。
> - 板型、大棚编号没有默认值，必须手动指定，以防误录。

---

### 2. ✏️ 记录编辑与删除 (新增)

如果你录入的数据填错了，可以进行修改或删除：

* **编辑记录**：
   - 在「录入」页面的下方展开「今日已录入」列表（或在「历史」页面展开指定日期的明细）。
   - 点击你要修改的数据旁边的 **✏️ 编辑按钮**。
   - 在弹出的对话框中修改产品、大棚、团队、数量等，然后点击「保存修改」。
* **删除记录**：
   - 在「今日已录入」列表中，点击 🗑️ 垃圾桶图标，可以删除单条产品记录。
   - 系统还支持 **「删除整板」**。点击板头部的「删除整板」会一次性删除该板及板内所有产品。

> ⚠️ **权限说明**：
> - 普通编辑人员 (Editor) 只能编辑或删除**自己录入的且是当天**的记录。
> - 管理员 (Admin) 和超级管理员 (Superadmin) 可以编辑或删除**任何日期、任何人的任何记录**。

---

### 3. 📅 历史数据与对账

* **今日/历史收菜明细**：
  - 点击底部的「今日」或「历史」按钮。
  - 顶部会自动显示**总产量**、**使用板数**和**包装筐数**（筐数等于各明细的总数量之和）。
  - 按产品进行折叠和统计，非常方便跟销售单或物流单对账。
* **区域分类情况**：
  - 点击底部的「区域」按钮。
  - 支持 **「导出单天汇总」** 和 **「导出周报 (周一至周日)」** (新增)。
  - **导出单天汇总**：不再显示各产品的明细流水，直接以汇总后的产品列出，大棚编号会自动合并展示（如 `GH04&GH05`）。
  - **导出周报**：自动汇总当前日期所在周（周一至周日）的全部成品菜数据，为财务和周度汇总提供无明细的两列极简汇总表。

---

### 4. 👑 管理员专有功能 (数据导入与清理)

**位置**：底部「更多」→「导入数据」

* **指定日期导入**：导入 Excel 时，可选择「全部强制覆盖为指定日期」，便于集中补单。
* **重复数据去重**：Excel 导入时系统会自动对比数据库。如果发现已有完全相同的数据（日期、产品、团队、箱型、大棚、数量完全一致），系统会在预览中以黄色标记并**自动跳过**，不再重复导入。
* **清除指定日期数据**：可彻底清空某一天的数据。点击后会显示该日期现存的数据条数，确认后方可清除。

---

## 🔑 权限角色对照表

| 角色 | 板级录入 | 查看报表与导出 | 编辑/删除他人记录 | 更改用户权限 | 批量导入/删除 |
| :--- | :---: | :---: | :---: | :---: | :---: |
| 👑 **超级管理员 (Betty)** | ✅ | ✅ | ✅ | ✅ (能管 Admin) | ✅ |
| 🛡️ **管理员 (Admin)** | ✅ | ✅ | ✅ | ✅ (不能管 Admin) | ✅ |
| ✏️ **编辑员 (Editor)** | ✅ | ✅ | ❌ (只能动自己的当天记录) | ❌ | ❌ |
| 👁️ **查看员 (Viewer)** | ❌ | ✅ | ❌ | ❌ | ❌ |

---

## ❓ 常见问题 FAQ

**Q：打板时，有一筐菜是不上板的（散装或直接装车），怎么录入？**
A：对于不上板的产品，在录入时直接单独打板即可，或者先随便选一个板型，提交后由管理员或自己点进编辑修改（或者后续由系统开发团队调整）。

**Q：为什么在用户管理中我无法修改某些人的角色？**
A：系统启用了安全控制保护。所有管理员平级互保，普通管理员无法降低其他管理员的权限，只有超级管理员 (Betty) 可以执行此操作。

**Q：我导出的 Excel 文件怎么和以前格式不一样？**
A：区域分类汇总表已经去掉了详细流水，只保留产品名称、包装数量、棚号及总数量。如果您需要查看详细流水，请使用管理员专用的「导出全部历史数据」功能。

---

## 📞 系统管理与技术支持

若系统使用中遇到异常，请联系系统开发者或管理员：
- ichsh48@gmail.com
- betty.huforwork@gmail.com
